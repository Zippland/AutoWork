# TaskPilot

独立的本地文件工作台。中文交流，简单实用；工程只处理交互、调度、执行器、验证及持久化。不要给模型增加固定知识分类、规划步骤、打分或能力白名单。

- 先读 README.md。源码 src/（界面）、core/（纯协议与调度）、server/（本地文件与执行）。资料保存在 ~/.TaskPilot，严禁提交用户数据。
- 仅保留工作与背景两个持久会话，互读已发布成果，分别写入各自目录。右侧聊天只对应工作助手；背景助手在“我的背景”页内对话。工作每轮先读最新背景。定时、手动推进/总结与直接对话才启动；卡片意见先保存，用户审阅完统一推进，运行中可继续保存下一批。卡片用自由 Markdown 总结，细节按需引用。审批与验收由宿主保存；模型不能伪造，也不能自行归档。
- 不修改当前仓库之外的工作台或自动化。改源码不触发真实模型烟测；测试使用隔离数据和受控 CLI。真实模型烟测需要明确授权并设置 TASKPILOT_RUN_REAL_AGENT_SMOKE=1。
- 默认 pnpm test、pnpm typecheck、pnpm lint、pnpm build；界面用隔离服务在浏览器验证。不要用真实事项测试审批、发送或归档。
- 本地执行器沿用用户原生登录、工具、权限与配置。失败保留草稿与可恢复记录，不重复执行结果不明的外部动作。
- 不覆盖其他人的修改，不提交凭据或私有资料，不添加 Co-Authored-By: Codex。
