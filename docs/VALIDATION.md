# 验证记录

2026-09-20，本地 macOS 环境：

- 136 项测试通过，覆盖文件协议与持久化、调度窗口、批次交接、审批版本、验收边界、并行发布、失败恢复、CLI 协议、Markdown 与附件阅读、倒计时显示。
- 背景通道可读取超过 100 条的原始工作历史与已提交意见，未提交和已撤回意见不混入；篡改或删除只读历史会阻止发布。受控轮次验证背景总结持久化后，工作通道在下一轮读取最新内容，且不额外加载整份历史。
- TypeScript 类型检查、ESLint、Vite 生产构建通过。
- 从暂存源码导出的干净副本执行 `pnpm install --frozen-lockfile`、全部检查、生产构建和 `pnpm start` 成功；使用空数据目录启动后进入连接 AI 的首次引导，无事项、对话或运行记录。
- 倒计时与三个操作按钮在 1000px、390px 宽度下核实为同一行、同一顶部位置，按钮高度均为 32px。
- 隔离浏览器验证：连续保存多卡意见后进入待推进；统一推进后进入跟进中；运行期间新意见留给下一轮。测试使用临时数据与受控 CLI。
- 隔离浏览器验证：倒计时按实际安排更新；暂停与关闭定时时显示对应状态。
- 附件阅读、全局看板/列表偏好和背景优先的首次引导已有隔离验证。

以上不等同于真实模型全流程、真实外部动作或用户验收。默认检查不接触真实账号、不发送消息、不批准或归档真实事项。GitHub CI 状态以对应提交的运行结果为准。

重新验证的命令与隔离运行方法见 [开发指南](DEVELOPMENT.md)。
